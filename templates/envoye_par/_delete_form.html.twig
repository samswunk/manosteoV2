<form method="post" action="{{ path('envoye_par_delete', {'id': envoye_par.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envoye_par.id) }}">
    <button class="btn">Delete</button>
</form>
