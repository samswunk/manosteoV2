<form method="post" action="{{ path('traumato_delete', {'id': traumato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traumato.id) }}">
    <button class="btn">Delete</button>
</form>
