{% extends 'index2/base2.html.twig' %}

{% block title %}Patient{% endblock %}

{% block body %}
    {% include ('patient/navbarPatient.html.twig') %}
    {{ include('patient/_form.html.twig', {'button_label': 'Update'}) }}
    {# <div class="col border border-danger row">
        {% block bouton %}{% endblock %}
        <a href="{{ path('patient_index') }}">back to list</a>
        {{ include('patient/_delete_form.html.twig') }}
    </div> #}
{% endblock body %}